riptide:
  defaults:
    retry:
      enabled: true
      backoff:
        enabled: true
        delay: 500 milliseconds
        max-delay: 3 seconds
        delay-factor: 3
    auth:
      credentials-directory: src/test/resources/credentials

  clients:
    example:
      auth.enabled: true
      retry:
        enabled: true
        backoff:
          enabled: true
          delay: 200 milliseconds
          max-delay: 5 seconds
          delay-factor: 2

