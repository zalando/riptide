riptide:
  defaults:
    retry:
      backoff:
        delay: 500 milliseconds
        max-delay: 3 seconds
        delay-factor: 3
  oauth:
    credentials-directory: src/test/resources

  clients:
    example:
      oauth.enabled: true
      retry:
        backoff:
          delay: 200 milliseconds
          max-delay: 5 seconds
          delay-factor: 2

